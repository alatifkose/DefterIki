[project]
name = "defteriki"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Abdüllatif Köse", email = "alatif.dev@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
defteriki = "defteriki:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "hypothesis>=6.168.0",
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
